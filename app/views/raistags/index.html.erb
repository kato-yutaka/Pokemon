<p id="notice"><%= notice %></p>

<h1>育成論タグ</h1>

<table>
  <thead>
    <tr>
      <th>育成論タグ名</th>
      <th>育成論ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @raistags.each do |raistag| %>
      <tr>
        <td><%= raistag.name %></td>
        <td><%= raistag.pokerais_id %></td>
        <td><%= link_to '詳細', raistag %></td>
        <td><%= link_to '編集', edit_raistag_path(raistag) %></td>
        <td><%= link_to '削除', raistag, method: :delete, data: { confirm: '本当に削除しますか?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_raistag_path %>
