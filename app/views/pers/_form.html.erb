<%= form_for(@per) do |f| %>
  <% if @per.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@per.errors.count, "error") %> prohibited this per from being saved:</h2>

      <ul>
      <% @per.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :up %><br>
    <%= f.text_field :up %>
  </div>
  <div class="field">
    <%= f.label :down %><br>
    <%= f.text_field :down %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
