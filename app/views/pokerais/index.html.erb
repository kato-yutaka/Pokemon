<div align = "center">
<p id="notice"><%= notice %></p>


<%
unless @pokedex
  @pokedex = Pokedex.new
end
%>

<h1>新着の育成論</h1>

<%= form_tag(:action=>"search") do %>
  <%= autocomplete_field_tag "search[title]",'',autocomplete_pokerai_title_pokerais_path,:class => '',:placeholder => '名前で探す'  %>
  <%= submit_tag "検索" %>
  <%= link_to "リセット", pokerais_path %>
 
<% end %></br>



    <table class="table table-striped">

    <thead>
      <tr>
        <th>タイトル</th>
        <th>画像</th>
        <th>投稿者名</th>
        <th>ポケモン名</th>
        <th>HP</th>
        <th>攻撃</th>
        <th>防御</th>
        <th>特功</th>
        <th>特防</th>
        <th>素早さ</th>
        <th>性格</th>
        <th>特性</th>
        <th>持ち物</th>
        <th>技１</th>
        <th>技２</th>
        <th>技３</th>
        <th>技４</th>

        <th>更新日時</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <% @pokerais.each do |pokerai| %>
	      <tr>
   <td><%= link_to pokerai.title , pokerai %></td>
					<td><%= image_tag show_image_pokedex_path(pokerai.pokedex_id) %></td>
					<td><%= link_to pokerai.user.name,user_path(pokerai.user_id)%></td>
  		    <td><%= link_to pokerai.pokedex.name,pokedex_path(pokerai.pokedex_id) %></td>
	        <td><%= pokerai.hp %></td>
					<td><%= pokerai.atk %></td>
	        <td><%= pokerai.def %></td>
	        <td><%= pokerai.satk %></td>
	        <td><%= pokerai.sdef %></td>
	        <td><%= pokerai.spd %></td>
					<td><%= link_to pokerai.per.name,per_path(pokerai.per_id)%></td>
					<td><%= link_to pokerai.cha.name,cha_path(pokerai.cha_id)%></td>
					<td><%= link_to pokerai.wepon.name,wepon_path(pokerai.wepon_id)%></td>
	        <% pokerai.skills.each do |skill| %>
						<% if pokerai.id == skill.pokerai_id %>
			        <td>|<%= link_to skill.skillname.name,skillname_path(skill.skillname_id) %>|</td>
 	  	 			<% end %>
 		   		<% end %>

	        <td><%= (pokerai.updated_at + 32400).to_s(:db) %></td>
     <% if "#{session[:usrad]}" ==  "true"%>
   		    <td><%= link_to '編集', edit_pokerai_path(pokerai) %></td>
    	   	<td><%= link_to '削除', pokerai, method: :delete, data: { confirm: '本当に削除しますか？' } %></td>
    <% end %>
      	 </tr>
    <% end %>
</table>
<br>
<% if "#{session[:usrname]}" == "" %>
           <li><a href="http://localhost:3000/login/index">育成論の投稿はログインするとできるようになります。</a></li>
<% else %>
<%= link_to '新規作成', new_pokerai_path %>
<% end %>