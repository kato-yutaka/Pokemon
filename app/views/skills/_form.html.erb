<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :育成論名 %><br>
    <%= f.collection_select :pokerai_id,Pokerai.all,:id,:title %>
  </div>
  <div class="field">
    <%= f.label :技名 %><br>
    <%= f.collection_select :skillname_id,Skillname.all,:id,:name %>
  </div>
  <div class="actions">
    <%= f.submit "登録" %>
  </div>
<% end %>
