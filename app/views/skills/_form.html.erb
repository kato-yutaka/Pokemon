<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :育成論ID %><br>
    <%= f.number_field :pokerais_id %>
  </div>
  <div class="field">
    <%= f.label :パーティポケモンID %><br>
    <%= f.number_field :party_id %>
  </div>
  <div class="field">
    <%= f.label :技ID %><br>
    <%= f.number_field :skillname_id %>
  </div>
  <div class="actions">
    <%= f.submit "登録" %>
  </div>
<% end %>
